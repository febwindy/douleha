<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.douleha.www.persistence.mybatis.mapper.IUserMapper">
    <select id="findById" resultType="com.douleha.www.domain.model.user.User">
        select * from douleha_user where userid = #{userId}
    </select>

    <resultMap id="authorityMap" type="Authority">
        <id column="authorityid" property="authorityId"/>
        <result column="authorityname" property="authorityName"/>
        <result column="authority_disabled" property="disabled" typeHandler="org.apache.ibatis.type.EnumOrdinalTypeHandler"/>
    </resultMap>
    <resultMap id="roleMap" type="Role">
        <id column="roleid" property="roleId"/>
        <result column="rolename" property="roleName"/>
        <result column="role_disabled" property="disabled" typeHandler="org.apache.ibatis.type.EnumOrdinalTypeHandler"/>
        <collection property="authorities" resultMap="authorityMap"/>
    </resultMap>
    <resultMap id="usernameMap" type="User">
        <id column="userid" property="userId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="salt" property="salt"/>
        <result column="nickname" property="nickname"/>
        <collection property="roles" resultMap="roleMap"/>
    </resultMap>
    <select id="findByUsername" resultMap="usernameMap">
        SELECT
            u.userid,
            u.username,
            u.`password`,
            u.salt,
            u.nickname,
            r.roleid,
            r.rolename,
            r.disabled AS role_disabled,
            a.authorityid,
            a.authorityname,
            a.disabled AS authority_disabled
        FROM
            douleha_user u
        LEFT JOIN douleha_user_role ur ON u.userid = ur.userid
        LEFT JOIN douleha_role r ON ur.roleid = r.roleid
        LEFT JOIN douleha_role_authority ra ON ra.roleid = r.roleid
        LEFT JOIN douleha_authority a ON a.authorityid = ra.authorityid
        WHERE
            u.username = #{username}
    </select>

    <insert id="save" parameterType="com.douleha.www.domain.model.user.User" useGeneratedKeys="true" keyProperty="userid">
        insert into douleha_user (userid, username, password, salt, nickname)
                VALUES (#{userId}, #{username}, #{password}, #{salt}, #{nickname})
    </insert>
</mapper>